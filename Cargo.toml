[package]
name = "solana-leader-precached"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = "0.8"
rand_chacha = "0.3"
tokio = { version = "1", features = ["full"] }
parking_lot = "0.12"
thiserror = "1.0"

[dev-dependencies]
tokio-test = "0.4"
